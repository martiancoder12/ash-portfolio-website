'use strict';
function teachCitation(ref){return `<a class="teach-citation" href="#engr-references">${esc(ref)}</a>`}
function teachRules(keys){return keys.map(key=>{const r=TEACH_RULES[key];return `<details class="teach-rule"><summary>Rule: ${r.name}</summary><p>${r.plain}</p>${engrMath(r.tex)}${teachCitation(r.ref)}</details>`}).join('')}
function teachStepHTML(s,i,current){return `<article class="step teaching-step ${current?'current':''}"><span class="step-number">${i+1}</span><div><h3>${s[0]}</h3><div class="teach-notice"><span>What I notice & why I move</span><p>${s[2]}</p></div><div class="teach-do"><span>What I write</span><div class="engr-step-math">${engrMath(s[1])}</div></div><div class="teach-rule-row">${teachRules(s[3])}</div><div class="teach-check"><strong>Quick check</strong><p>${s[4]}</p></div></div></article>`}
function teachRoadmap(method='separable'){const parts=method==='choose'?['Name x and the unknown y','Test the linear door','Test the separation door','Choose an open door','Check before dividing','Follow the recipe & verify']:method==='linear'?['Put 1 in front of y′','Find P and the helper μ','Multiply every term','Use the product rule','Integrate, then divide','Use any given pin; check']:['Find x-only × y-only','Check before dividing','Sort the variables','Integrate both sides','Use a pin, if given','Check answer & interval'];return `<div class="teach-roadmap"><div class="eyebrow">THE PROCESS · ONE SMALL MOVE AT A TIME</div><ol>${parts.map((s,i)=>`<li><span>${i+1}</span>${s}</li>`).join('')}</ol></div>`}
function teachGlossary(){return `<details class="teach-glossary"><summary>Start here if the symbols feel unfamiliar</summary><dl><div><dt>x</dt><dd>The input: where you are along the horizontal axis. In a time story, it plays the role of time.</dd></div><div><dt>y(x)</dt><dd>The value we want to find at each x: a height, amount, or other changing quantity.</dd></div><div><dt>y′ = dy/dx</dt><dd>The slope: how fast y changes as x changes. A flat line has slope 0.</dd></div><div><dt>∫ · dx</dt><dd>Integrate with respect to x. Here, we use it to undo a derivative.</dd></div><div><dt>C</dt><dd>One fixed number that chooses one curve from a family. C stays fixed along that curve.</dd></div><div><dt>Initial condition (IC)</dt><dd>A starting pin, such as y(0) = 3: the curve must pass through (0, 3).</dd></div><div><dt>μ, pronounced “mu”</dt><dd>A helper we multiply by so the left side becomes one product derivative.</dd></div><div><dt>e and ln</dt><dd>e is the exponential base, about 2.718. ln undoes e raised to a power. The input to ln must be positive.</dd></div><div><dt>Domain / interval</dt><dd>The x-values on which the answer is allowed to work. Avoid holes such as division by zero.</dd></div><div><dt>y꜀ and yₚ</dt><dd>The zero-input (homogeneous) part and one input-on (particular) solution. They add to form the linear solution.</dd></div></dl>${teachCitation('Zill (2018, pp. 4–8, 14–15, 43–44, 51–52).')}</details>`}
function teachSourceBlock(reference){return `<aside class="teach-source"><p>${teachCitation(reference)}</p><details><summary>APA reference & how to find this passage</summary><p class="apa-reference">${ZILL_APA}</p><p>Page numbers above are the book’s printed numbers. In your PDF, printed page 45 is PDF page 66. The inside-front-cover rule tables are unnumbered: “Differentiation Rules” is PDF page 4 and “Integration Formulas” is PDF page 5.</p><p>The analogies and step-by-step wording are original teaching explanations. References support the mathematical method; a practice-set label does not imply that the exercise appears verbatim in the textbook.</p></details></aside>`}
function renderEngrReferences(){document.title='Textbook references · ENGR 213';$('#course-label').textContent='ENGR 213 / RULES & REFERENCES';main.innerHTML=`${engrSwitch()}<div class="eyebrow">THE REASONS BEHIND THE MOVES</div><h1>One rule. One reason.</h1><p class="subtitle">Open a rule to see what it allows, why it helps, and where to find it in your textbook.</p><div class="teach-bibliography"><h2>APA reference</h2><p class="apa-reference">${ZILL_APA}</p><p>Your PDF’s copyright page says ©2018 and identifies the sixth edition. The 2016 in the filename is not used as the citation year. In-text citations use printed page numbers.</p></div><div class="teach-rule-library">${teachRules(Object.keys(TEACH_RULES))}</div><div class="teach-bibliography"><h2>Where the main ideas live</h2><ul><li><strong>What counts as a solution:</strong> §1.1, pp. 4–8.</li><li><strong>Starting points:</strong> §1.2, pp. 14–15.</li><li><strong>Slope fields:</strong> §2.1.1, pp. 34–35.</li><li><strong>Constant / equilibrium solutions:</strong> §2.1.2, p. 36; §2.2, pp. 45–46.</li><li><strong>Separation and renaming constants:</strong> §2.2, pp. 43–44.</li><li><strong>Standard form and y꜀ + yₚ:</strong> §2.3, pp. 50–51.</li><li><strong>Integrating-factor process:</strong> §2.3, p. 52.</li><li><strong>Intervals for linear equations:</strong> §2.3, pp. 53–54.</li><li><strong>The tap-on / tap-off example:</strong> §2.3, pp. 54–55, Example 5.</li></ul><p>Practice problems S1–S5, L1–L5 and drills M1–M6 come from your supplied practice set. L1 matches Zill’s Example 1 on p. 52; L5 matches Example 5 on p. 55. Other citations identify supporting methods, not a claim that the same numbered problem appears in Zill.</p><p>The scientific explanations and analogy wording here are paraphrases. The textbook itself is not uploaded to this website.</p></div><a href="#engr213">← Back to the practice desk</a>`;animate('.teach-bibliography')}
let constantC=0;
function renderConstantCurves(){document.title='Why C = 0 gives y = −1 · ENGR 213';$('#course-label').textContent='ENGR 213 / CONSTANT SOLUTIONS';main.innerHTML=`${engrSwitch()}<div class="eyebrow">SEE THE WHOLE FAMILY</div><h1>One family. One perfectly flat curve.</h1><p class="subtitle">Think of C as the curve selector. Change C to choose a different curve; once chosen, C stays fixed as x changes.</p><div class="engr-equation">${engrMath(R`y'=x(y+1),\qquad y=Ce^{x^2/2}-1`)}</div><div class="constant-lab"><div class="constant-controls"><label for="constant-slider">Curve selector C: <output id="constant-output">0</output></label><input id="constant-slider" type="range" min="-1" max="1" step="0.05" value="${constantC}"><div><button data-constant="-1">C = −1</button><button data-constant="0">C = 0 · flat</button><button data-constant="1">C = +1</button></div></div><div id="constant-plot"></div><div class="constant-readout" id="constant-readout" aria-live="polite"></div></div><div class="constant-explanation"><article><span>01 · THE FLAT ROAD</span><h2>C = 0 removes the changing piece.</h2>${engrMath(R`y=0\cdot e^{x^2/2}-1=-1`)}<p>No matter which x you choose, the height is −1. The whole function is constant, so its derivative is 0.</p>${teachRules(['constant'])}</article><article><span>02 · THE ORIGINAL RULE ACCEPTS IT</span><h2>Its slope really is the required slope.</h2>${engrMath(R`y'=0,\qquad x(y+1)=x(-1+1)=0`)}<p>Both sides are zero for every x. That is the test for being a solution.</p>${teachRules(['verify'])}</article><article><span>03 · DIVISION CAN HIDE IT</span><h2>Save the flat road before dividing.</h2>${engrMath(R`\frac{y'}{y+1}=x\quad\text{requires }y+1\ne0`)}<p>At y = −1, that fraction would be 0/0. The original equation is fine; this divided form excludes the flat curve. Check it separately, then restore it in the final family with C = 0.</p>${teachRules(['lost','balance'])}</article></div>${teachSourceBlock('Zill (2018, pp. 45–46, “Losing a Solution”; pp. 51–53 for linear solutions).')}<div class="next-row"><a href="#M1">Follow every algebra step →</a><a href="#engr213">Back to the ENGR desk</a></div>`;$('#constant-slider').oninput=e=>{constantC=+e.target.value;drawConstantCurves()};main.querySelectorAll('[data-constant]').forEach(b=>b.onclick=()=>{constantC=+b.dataset.constant;$('#constant-slider').value=constantC;drawConstantCurves()});drawConstantCurves();}
function drawConstantCurves(){const host=$('#constant-plot');if(!host)return;const width=Math.max(300,host.clientWidth||700),height=340,left=54,right=24,top=28,bottom=45,plotW=width-left-right,plotH=height-top-bottom,X=x=>left+(x+1.5)/3*plotW,Y=y=>top+(2.5-y)/7*plotH;const line=c=>{let d='';for(let i=0;i<=160;i++){const x=-1.5+3*i/160,y=c*Math.exp(x*x/2)-1;d+=(i?'L':'M')+X(x).toFixed(2)+','+Y(y).toFixed(2)}return d};let s=`<rect x="${left}" y="${top}" width="${plotW}" height="${plotH}" fill="none" stroke="#dce4df"/>`;for(const y of [-4,-2,-1,0,2])s+=`<line x1="${left}" x2="${width-right}" y1="${Y(y)}" y2="${Y(y)}" stroke="#e4ebe6"/><text x="${left-10}" y="${Y(y)+4}" text-anchor="end">${y}</text>`;for(const x of [-1.5,0,1.5])s+=`<text x="${X(x)}" y="${height-24}" text-anchor="middle">${x}</text>`;s+=`<text x="${left+plotW/2}" y="${height-4}" text-anchor="middle">x</text><text x="19" y="${top+plotH/2}" text-anchor="middle">y</text>`;for(const c of [-1,0,1])s+=`<path d="${line(c)}" fill="none" stroke="${c===0?'#a87336':'#93afa0'}" stroke-width="${c===0?2:1.5}" ${c===0?'stroke-dasharray="6 4"':''}/>`;s+=`<path id="constant-active-curve" d="${line(constantC)}" fill="none" stroke="#00695c" stroke-width="3.5"/><circle cx="${X(0)}" cy="${Y(constantC-1)}" r="5" fill="#00695c" stroke="white" stroke-width="2"/>`;for(const c of [-1,0,1]){const xx=1.22,yy=c*Math.exp(xx*xx/2)-1;s+=`<text x="${X(xx)}" y="${Y(yy)+(c===-1?20:-10)}" text-anchor="end">C = ${c===1?'+1':c}</text>`;}host.innerHTML=`<svg viewBox="0 0 ${width} ${height}" role="img" aria-label="Family y equals C times exp(x squared over two) minus one. C = 0 is the flat line y = −1. Faint curves show C = −1 and +1; the solid selected curve uses C = ${constantC}.">${s}</svg>`;$('#constant-output').textContent=fmt(constantC,2);$('#constant-readout').innerHTML=constantC===0?'<strong>C = 0 → y is always −1 → y′ is always 0.</strong> The selected curve is a perfectly flat road.':`<strong>C = ${fmt(constantC)} → y(0) = ${fmt(constantC-1)}.</strong> The curve ${constantC>0?'stays above':'stays below'} y = −1. C is constant, but y changes as x changes.`;main.querySelectorAll('[data-constant]').forEach(b=>b.setAttribute('aria-pressed',String(+b.dataset.constant===constantC)));}
window.addEventListener('resize',()=>{if(typeof activeId!=='undefined'&&activeId==='engr-curves')drawConstantCurves()});
