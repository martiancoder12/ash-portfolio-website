'use strict';
function elecRoute(id){return id==='elec-references'||/^elec-P[1-5]-[ab]$/.test(id)||id==='elec275-lectures'||/^elec275-lecture-\d+$/.test(id)}
function elecSwitch(){return `<nav class="desk-switch" aria-label="Study desks"><a href="#engr213">ENGR 213 <span>Differential equations ↗</span></a><a href="#comp232-notes">COMP 232 <span>Discrete mathematics ↗</span></a><a href="#quiz" class="selected" aria-current="page">ELEC 275 <span>Circuit analysis</span></a></nav>`}
function elecCitation(ref){return `<a class="teach-citation" href="#elec-references">${esc(ref)}</a>`}
function elecRules(keys){return keys.map(key=>{const r=ELEC_RULES[key];return `<details class="teach-rule"><summary>Rule: ${r.name}</summary><p>${r.plain}</p>${engrMath(r.tex)}${elecCitation(r.ref)}</details>`}).join('')}
function elecStepHTML(s,i,current){return `<article class="step teaching-step ${current?'current':''}"><span class="step-number">${i+1}</span><div><h3>${s[0]}</h3><div class="teach-notice"><span>What I notice & why I move</span><p>${s[2]}</p></div><div class="teach-do"><span>What I write</span><div class="engr-step-math">${engrMath(s[1])}</div></div><div class="teach-rule-row">${elecRules(s[3])}</div><div class="teach-check"><strong>Quick check</strong><p>${s[4]}</p></div></div></article>`}
function elecRoadmap(method='laws'){
 const routes={
  laws:['Name the target','Mark arrows & + signs','Find a junction or loop','Write KCL / KVL + Ohm','Solve one unknown','Check the original circuit'],
  nodal:['Pick a zero level','Name unknown node voltages','Write (start − end) / R','Balance each node: KCL','Solve the equations','Find targets; check signs'],
  reduce:['Name the target','Trace the shared endpoints','Close a series / parallel box','Find the source current','Reopen boxes in reverse','Check the original circuit'],
  series:['Check: no fork between them','Add the series resistances','Find the shared current','Target R / total R','Find the target voltage','Check the voltage budget'],
  parallel:['Check: same two endpoints','Find the common voltage','Find total current if needed','Other R / total R','Find the target current','Check the current split'],
  source:['What does the source fix?','Simplify the load','Use V = IReq on the load','Reopen the branches','Find the requested value','Check KCL & KVL'],
  units:['Name the wanted quantity','Write the matching law','Keep each unit attached','Convert or cancel prefixes','Calculate the number','Check the size & unit']
 };
 return `<div class="teach-roadmap elec-roadmap"><div class="eyebrow">THE PROCESS · ONE SMALL MOVE AT A TIME</div><ol>${(routes[method]||routes.laws).map((s,i)=>`<li><span>${i+1}</span>${s}</li>`).join('')}</ol></div>`;
}
function elecGlossary(){return `<details class="teach-glossary"><summary>Start here: what are voltage, current and all these symbols?</summary><dl><div><dt>Current I · amperes (A)</dt><dd>How much charge passes a point each second. Picture a flow. An arrow chooses the direction we call positive; a negative result means the other way.</dd></div><div><dt>Voltage V · volts (V)</dt><dd>Energy difference per unit charge between TWO points. Picture a height difference. VA means A’s voltage relative to our chosen zero.</dd></div><div><dt>Resistance R · ohms (Ω)</dt><dd>How much a resistor opposes current. At the same voltage, a larger resistance allows less current.</dd></div><div><dt>Node · junction</dt><dd>A whole region joined by ideal wire, with no component between its points. Every point on it has the same voltage. B can label a long wire, not just one dot.</dd></div><div><dt>Ground / reference · 0 V</dt><dd>The level we choose to call zero. It is a measuring reference; it does not mean current vanishes there.</dd></div><div><dt>Series · +</dt><dd>One unbranched path through consecutive components. They carry the same current. Their resistances add.</dd></div><div><dt>Parallel · ∥</dt><dd>Separate paths joining the SAME TWO nodes. They have the same voltage across them. Their currents add.</dd></div><div><dt>Equivalent · Req</dt><dd>A simpler stand-in with the same voltage–current relationship at the outside terminals. Keep the original diagram so you can reopen it later.</dd></div><div><dt>kΩ and mA</dt><dd>k = 1,000; m = 1/1,000. With V and kΩ, Ohm’s law gives mA. Thus 2 mA × 8 kΩ = 16 V.</dd></div><div><dt>Subscripts: V3, I5, VB</dt><dd>The small label says which object we mean: voltage across R3, current through R5, or voltage at node B relative to zero. These are different targets.</dd></div><div><dt>KCL · junction rule</dt><dd>Current in equals current out. If you choose all arrows to leave, add their SIGNED values to zero.</dd></div><div><dt>KVL · loop rule</dt><dd>Walk a closed circuit loop. Add voltage rises and subtract voltage drops: the total change is zero.</dd></div></dl><p>Flow and height are pictures to help you remember. The circuit laws below are what we use to calculate. These lessons use ideal wires, ideal sources and steady DC resistor circuits.</p>${elecCitation('Rizzoni and Kearns (2022, pp. 5–6, 11–19, 22–23, 29–30, 39–41, 80–91).')}</details>`}
function elecSourceBlock(reference){return `<aside class="teach-source"><p>${elecCitation(reference)}</p><details><summary>APA reference & how to find this passage</summary><p class="apa-reference">${ELEC_APA}</p><p>In-text citations use the book’s printed page numbers. Your PDF is reflowed: printed p. 22 is PDF p. 71; printed p. 39 starts on PDF p. 100. There is no single page offset. <a href="#elec-references">Open the topic-to-PDF-page map →</a></p><p>The analogies and small-step explanations are original paraphrases. P1–P5 and D1–D6 refer to your supplied practice set; the citations identify the supporting textbook methods.</p></details></aside>`}
function elecPowerAudit(){return `<details class="elec-power audit"><summary>One more independent check: where did the 72 mW go?</summary><p>Think of power as the energy used each second. For each resistor, multiply its own voltage drop by its own current. The current enters the + terminal, so each resistor absorbs positive power.</p>${engrMath(R`P=VI,\qquad \mathrm V\times\mathrm{mA}=\mathrm{mW}`)}<div class="elec-table-wrap"><table><caption>P4 · each original resistor’s power</caption><thead><tr><th>Part</th><th>Voltage drop</th><th>Current</th><th>Absorbed power</th></tr></thead><tbody><tr><th>R1</th><td>12 V</td><td>4 mA</td><td>48 mW</td></tr><tr><th>R2</th><td>4 V</td><td>2 mA</td><td>8 mW</td></tr><tr><th>R3</th><td>8 V</td><td>4/3 mA</td><td>32/3 mW</td></tr><tr><th>R4</th><td>4 V</td><td>2/3 mA</td><td>8/3 mW</td></tr><tr><th>R5</th><td>4 V</td><td>1/3 mA</td><td>4/3 mW</td></tr><tr><th>R6</th><td>4 V</td><td>1/3 mA</td><td>4/3 mW</td></tr></tbody></table></div>${engrMath(R`P_{\rm resistors}=48+8+\frac{32+8+4+4}{3}=72\,\mathrm{mW}`)}<p>The source sends 6 mA OUT of its + terminal. For the passive sign convention, current INTO its + terminal is −6 mA. Its power is therefore negative: it supplies energy.</p>${engrMath(R`P_{\rm source}=(12)(-6)=-72\,\mathrm{mW},\qquad \sum P=-72+72=0`)}<p><strong>Why this helps:</strong> all supplied power is accounted for. It is an extra check, alongside KCL, KVL and Ohm’s law.</p>${elecRules(['power','energy','sign','units'])}</details>`}
function renderElecDesk(){
 document.title='ELEC 275 · One small step at a time';$('#course-label').textContent='ELEC 275 / QUIZ 1 PRACTICE';
 main.innerHTML=`${elecSwitch()}<div class="eyebrow"><span class="tag">QUIZ 1</span> YOUR PRACTICE DESK</div><h1>Make one small move.<br>Know why it works.</h1><p class="subtitle">A circuit is a few paths, a few junctions, and rules that keep the numbers honest. Start with a simple picture. Follow the process. Check the result in the original circuit.</p><div class="quiz-path"><a href="#elec-P1-a"><span>01</span> Build the basics <strong>Start with one loop →</strong></a><a href="#elec-P4-b"><span>02</span> Tackle the ladder <strong>Close & reopen boxes →</strong></a><a href="#elec-references"><span>03</span> Understand each rule <strong>Textbook & APA references →</strong></a></div><p class="elec-notes-link"><a href="#elec275-notes">Textbook notes · Chapter 1 →</a></p>${elecRoadmap()}${elecGlossary()}<div class="study-rule"><strong>Your checklist:</strong> What am I finding? Which two endpoints or path matter? Which law is allowed here? What does the sign mean? Does it fit the original circuit? <p>These are the questions from your supplied practice set, with two complete methods for each. Use them to practise the process for the quiz.</p></div><div class="problem-list">${ELEC_PRACTICE.map((p,i)=>`<details class="problem" ${i===0?'open':''}><summary><span class="problem-id">${p.n}</span><span>${p.title}<small>${p.targets}</small></span><span class="problem-open">+</span></summary><div class="problem-content"><p>${p.givens}</p><div class="problem-diagram">${quizDiagram(p.n)}</div><p class="elec-thinking"><strong>First question:</strong> ${p.thought}</p><div class="method-pair"><details><summary>Method (a) · Write the circuit laws</summary><p>Mark the voltages and arrows. Use Ohm’s law to describe each current, then balance a junction or loop. Follow ${p.a.length} small moves.</p><a class="elec-start" href="#elec-${p.n}-a">Work through method (a) →</a></details><details><summary>Method (b) · Simplify, then reopen</summary><p>Check each connection before combining resistors. Find the overall flow, then recover the original branch values. Follow ${p.b.length} small moves.</p><a class="elec-start" href="#elec-${p.n}-b">Work through method (b) →</a></details></div><details class="audit"><summary>Check your final answers</summary>${engrMath(p.answer)}<p>${p.check}</p></details><a href="#${p.lesson}">Explore the matching interactive lesson →</a></div></details>`).join('')}</div><section class="speed-drill"><div class="eyebrow">BUILD THE REFLEX · SPEED COMES LATER</div><h2>Six small practice checks.</h2><p class="recall-note">Name the connection and the rule first. Work it out on paper, then open the explanation. Once the process feels familiar, try a 30-second timer.</p><div class="drill-grid">${ELEC_DRILLS.map(d=>`<details><summary><span>${d.id}</span>${d.q}</summary><strong>${d.answer}</strong><p>${d.why}</p>${d.steps.map((s,i)=>elecStepHTML(s,i,false)).join('')}</details>`).join('')}</div></section><section class="exam-map"><h2>Leave a trail someone else can follow.</h2><ol><li><strong>Label:</strong> name nodes, current arrows and voltage polarities.</li><li><strong>State the rule:</strong> write “KCL at B,” “KVL around this loop,” or the series / parallel condition.</li><li><strong>Show one move per line:</strong> substitute, simplify, then solve. Keep fractions exact until the end.</li><li><strong>Answer the target:</strong> box the requested voltage or current, including its unit and reference direction.</li><li><strong>Check:</strong> currents balance, loop voltages add to zero, and a positive parallel equivalent is smaller than every branch resistance.</li></ol></section>${elecSourceBlock('Rizzoni and Kearns (2022, §§1.2–1.7 and §§2.1–2.3).')}<p class="recall-note">Unit correction in the supplied practice material: mA × Ω gives mV (millivolts), not μV (microvolts). <a href="#E8">See the prefix cancellation →</a></p>`;
 animate('.quiz-path a');
}
function renderElec(){if(activeId==='elec275-lectures'||/^elec275-lecture-\d+$/.test(activeId))return renderElec275Lectures();if(activeId==='elec-references')return renderElecReferences();return renderElecProblem()}
function renderElecProblem(){
 const [,n,method]=activeId.split('-'),p=ELEC_PRACTICE.find(p=>p.n===n),steps=p[method];
 document.title=`${n} · Method (${method}) · ELEC 275`;$('#course-label').textContent=`ELEC 275 / ${n} / METHOD (${method.toUpperCase()})`;
 main.innerHTML=`${elecSwitch()}<a class="elec-back" href="#quiz">← All five practice problems</a><div class="eyebrow">${n} · ONE SMALL MOVE AT A TIME</div><h1>${p.title}</h1><p class="subtitle">${p.targets}</p><div class="elec-method-tabs" aria-label="Choose a solution method"><a href="#elec-${n}-a" ${method==='a'?'aria-current="page"':''}>Method (a) · Circuit laws</a><a href="#elec-${n}-b" ${method==='b'?'aria-current="page"':''}>Method (b) · Reduction & dividers</a></div><div class="story"><div class="story-label">PICTURE IT</div><p>${p.story}</p></div><div class="lab"><div class="lab-head"><div><h2>Keep the original circuit in view.</h2><p>${p.givens}</p></div></div><div class="problem-diagram" tabindex="0" aria-label="Original circuit; scroll sideways on small screens">${quizDiagram(n)}</div><p class="elec-diagram-caption">Use the bottom rail as 0 V. Voltage is measured between two points; current goes through a path. Scroll sideways to inspect all labels on a small screen.</p></div><p class="elec-thinking"><strong>First question:</strong> ${p.thought}</p>${elecRoadmap(method==='a'?(n==='P1'?'laws':n==='P2'?'parallel':'nodal'):p.method)}${elecGlossary()}<div class="study-rule"><strong>Our unit shortcut:</strong> when the numbers use volts and kilohms, currents come out in milliamperes. For example, 12 V ÷ 6 kΩ = 2 mA. We keep those units consistent in the node equations.</div><section aria-label="Worked solution"><div class="elec-walk-header"><h2>Method (${method}) · ${method==='a'?'Write the laws':'Simplify and reopen'}</h2><button id="elec-hide" aria-expanded="true" aria-controls="elec-work">Hide the work · try from memory</button></div><div id="elec-work"><p class="worked-intro">Read the reason, predict the calculation, then reveal the next move. Open a rule to see what permits that move.</p><div class="step-list" id="elec-step-list"></div><div class="step-toolbar"><span id="elec-step-count" aria-live="polite"></span><button id="elec-step-back">← Back</button><button id="elec-step-all">Reveal all steps</button><button class="primary" id="elec-step-next">Reveal next step →</button></div></div><p id="elec-memory-note" hidden>On paper: redraw the circuit, name the target, write the governing equations, and solve. Say which rule permits each move. Then compare your result below.</p></section><details class="audit elec-answer"><summary>Check the final answers in the original circuit</summary>${engrMath(p.answer)}<p>${p.check}</p>${elecRules(['ohm','kcl','kvl'])}</details>${n==='P4'?elecPowerAudit():''}${elecSourceBlock(p.source)}<div class="next-row"><a href="#elec-${n}-${method==='a'?'b':'a'}">Try the other method →</a><a href="#${p.lesson}">Explore the interactive circuit →</a></div>`;
 const draw=enter=>{step=Math.min(step,steps.length-1);$('#elec-step-list').innerHTML=steps.slice(0,step+1).map((s,i)=>elecStepHTML(s,i,i===step)).join('');$('#elec-step-count').textContent=`Step ${step+1} of ${steps.length}`;$('#elec-step-back').disabled=step===0;$('#elec-step-next').disabled=step===steps.length-1;$('#elec-step-all').disabled=step===steps.length-1;if(enter&&motion&&window.gsap)gsap.from('#elec-step-list .step:last-child',{opacity:0,y:12,duration:.4})};
 $('#elec-step-next').onclick=()=>{step=Math.min(step+1,steps.length-1);draw(true)};$('#elec-step-back').onclick=()=>{step=Math.max(0,step-1);draw(false)};$('#elec-step-all').onclick=()=>{step=steps.length-1;draw(true)};
 $('#elec-hide').onclick=()=>{const work=$('#elec-work'),hidden=!work.hidden;work.hidden=hidden;$('#elec-memory-note').hidden=!hidden;$('#elec-hide').setAttribute('aria-expanded',String(!hidden));$('#elec-hide').textContent=hidden?'Show the worked solution':'Hide the work · try from memory'};draw(false);
}
function renderElecReferences(){
 document.title='Textbook rules & references · ELEC 275';$('#course-label').textContent='ELEC 275 / RULES & REFERENCES';
 const rows=[['SI prefixes','Preface, p. xiv, Tables 0.1–0.2','28–29'],['Nodes and ideal connections','§1.1, pp. 5–6','43–44'],['Current and voltage','§1.2, pp. 11–13','51–54'],['Ideal sources','§1.3, pp. 14–16','55–60'],['Passive sign convention and power balance','§1.4, pp. 17–21','61–69'],['KCL and KVL','§1.5, pp. 22–23','70–73'],['Negative current means opposite direction','§1.5, pp. 25–26','75–76'],['Ohm’s law','§1.6, pp. 29–30','81–82'],['Node-voltage method','§1.7, pp. 39–41','100–103'],['Series resistance and voltage division','§2.1, pp. 80–82','169–172'],['Parallel resistance and current division','§2.2, pp. 88–91','183–186'],['Equivalent resistance / repeated replacement','§2.3, pp. 93–94','190–191']];
 main.innerHTML=`${elecSwitch()}<div class="eyebrow">THE REASONS BEHIND THE MOVES</div><h1>Every move has a reason.</h1><p class="subtitle">A rule tells you what you may do. Its condition tells you when you may do it. Use this shelf whenever a step feels like a magic trick.</p><div class="teach-bibliography"><h2>APA reference</h2><p class="apa-reference">${ELEC_APA}</p><p>Your textbook’s title page identifies the seventh edition, and its copyright page says ©2022. In-text citations here use printed page numbers, for example: (Rizzoni & Kearns, 2022, p. 22).</p></div><div class="teach-bibliography"><h2>Find the passage in your PDF.</h2><p>Your PDF reflows the book over more pages. The printed number and PDF viewer number are different, and there is no fixed offset. The following ranges locate the relevant discussions.</p><div class="elec-table-wrap"><table><caption>Textbook topic map · PDF page numbers start at 1</caption><thead><tr><th>Topic</th><th>Printed reference</th><th>PDF pages</th></tr></thead><tbody>${rows.map(r=>`<tr>${r.map((v,i)=>i===0?`<th>${v}</th>`:`<td>${v}</td>`).join('')}</tr>`).join('')}</tbody></table></div></div><h2>The circuit rules and the algebra underneath.</h2><div class="teach-rule-library">${elecRules(Object.keys(ELEC_RULES))}</div><div class="teach-bibliography"><h2>How the sources are used</h2><p>P1–P5 and D1–D6 come from your supplied <em>ELEC275 Quiz1 Practice Set</em>. Textbook citations support the circuit principles and methods; they do not claim these practice labels are the textbook’s exercise numbers.</p><p>The stories, process maps and step-by-step wording are original paraphrases. Basic algebra moves are identified as algebra, while circuit laws are attributed to their textbook sections. The textbook file itself is not uploaded to this site.</p><p>The supplied practice set’s “mA × Ω = μV” is corrected to “mA × Ω = mV.” KCL always uses a signed sum, and P4’s source-to-B resistor is R2.</p></div><a href="#quiz">← Back to the ELEC 275 practice desk</a>`;
 animate('.teach-bibliography');
}
