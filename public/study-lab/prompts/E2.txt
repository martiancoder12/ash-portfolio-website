Create a flat-vector educational diagram titled "Series or Parallel? The Two Identity Tests". Two panels with flat zigzag-resistor schematics. Left panel "SERIES — same current": two resistors end to end on one wire with a single continuous current arrow passing through both, a big green check mark, formula "Req = R1 + R2", and caption "the current leaving one has nowhere else to go but into the other". Right panel "PARALLEL — same voltage": two resistors on vertical branches between two horizontal rails, with node dots at all four junctions, formula "1/Req = 1/R1 + 1/R2", and caption "both terminals touch the SAME two nodes". A red-bordered warning strip across the bottom: "TRAP — if a third branch taps the middle wire, they are NOT in series."

Style: clean flat-vector educational infographic, white background, generous whitespace, muted academic palette (dark slate #263238, teal #00695C, warm grey #78909C, soft fill #EEF3F6), crisp sans-serif labels, no 3D, no photorealism, no gradients, no decorative clutter. Render ALL text exactly as written between quotes — do not invent extra text or change any formula. Aspect ratio 16:9.

Final revision instructions (applied to generated draft):
Edit this educational infographic only to fill the entire 16:9 canvas with opaque solid pure white, including all empty areas between and outside panels, so every title and diagram is clear against white. Preserve all existing correct text, math, labels, layout, diagram connections, sizes and teal/slate colors. Do not use transparency, alpha cutouts, shadows or gradients.
