Generate this figure by running Python (matplotlib) — do NOT draw it as an illustration. Plot y = 2/(1 − 2 sin x) on the open interval (−7π/6, π/6), the solution of y' = y² cos x with y(0) = 2. Mark the point (0, 2) with a dot labeled "y(0) = 2". Draw vertical dashed asymptotes at x = −7π/6 and x = π/6, each labeled "blow-up". Lightly shade the regions outside the interval in pale red with the caption "outside the interval of validity". Title: "A nonlinear solution can escape in finite time". White background, axes labeled, y-axis limited to [−1, 10].
