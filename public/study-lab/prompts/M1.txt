Create a flat-vector educational flowchart titled "First-Order ODE Triage". Layout top to bottom: start box reading "dy/dx = ?". Decision diamond 1: "Do y and y' appear only to the first power, never inside functions, never multiplied together?" YES branch goes right to a green box: "LINEAR → standard form y' + P(x)y = f(x) → μ = e^∫P dx". Continue down to decision diamond 2: "Does the right side factor as g(x)·f(y)?" YES branch goes right to a teal box: "SEPARABLE → dy/f(y) = g(x) dx → integrate both sides". A path where both answers are YES leads to a box "BOTH — pick the easier route"; both NO leads to a grey box "NEITHER (needs other tools)". Along the bottom, four small example chips: "y' = xy + x → both", "y' + x²y = x³ → linear only", "yy' = x → separable only", "y' = sin(x+y) → neither".

Style: clean flat-vector educational infographic, white background, generous whitespace, muted academic palette (dark slate #263238, teal #00695C, warm grey #78909C, soft fill #EEF3F6), crisp sans-serif labels, no 3D, no photorealism, no gradients, no decorative clutter. Render ALL text exactly as written between quotes — do not invent extra text or change any formula. Aspect ratio 16:9.

Final revision instructions (applied to generated draft):
Edit this educational image to correct the decision tree logic, while preserving the title, all four example chips, exact decision questions, formula boxes, muted slate/teal colors and typography. Use this unambiguous binary tree: start dy/dx = ? at top; first decision tests linearity using existing exact long question. Its YES branch leads to a copy of the second question "Does the right side factor as g(x)·f(y)?" and its NO branch leads to a separate second copy of this exact same question. From the second question reached through first YES, second YES must lead ONLY to "BOTH — pick the easier route", second NO must lead ONLY to the existing LINEAR formula box. From the second question reached through first NO, second YES must lead ONLY to the existing SEPARABLE formula box, second NO must lead ONLY to "NEITHER (needs other tools)". Every edge clearly labeled YES or NO; four final leaves with no crossing edges. Do not route NO then YES to BOTH. Retain all four existing example chips, unchanged. Fit comfortably in a 16:9 canvas. Fill the entire canvas opaque solid white including spaces between all elements. No transparency, no alpha cutouts, no gradients, no glow or shadow.
