Generate this figure by running Python (matplotlib) — do NOT draw it as an illustration. Draw the slope field of dy/dx = x/y on a grid x in [−4, 4], y in [−4, 4]: at each grid point a short line segment with slope x/y. Overlay three solution curves of the family y² − x² = C for C = −4, 0, +4 in teal, so the hyperbolas visibly follow the field. Add a small zoom callout on one segment reading "each segment has slope x/y at its point". Title: "Slope field of y' = x/y — solutions ride the field". Equal aspect ratio, axes labeled, white background.
