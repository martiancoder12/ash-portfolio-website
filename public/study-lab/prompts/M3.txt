Create a flat-vector educational diagram titled "The Integrating Factor: Why μ Works". Two large panels. Left panel "THE PROBLEM": equation "y' + P(x)y = f(x)" with the left side circled in red, caption "not the derivative of anything — cannot integrate directly". Right panel "THE TRICK": the same equation with every term multiplied by a glowing teal highlighted factor "μ = e^∫P dx", and a big arrow showing the left side collapsing into one compact box "(μy)'", with a verification bubble: "product-rule check: (μy)' = μy' + μ'y = μy' + μPy ✓". Bottom strip: five small numbered chips in a row: "1 standard form" → "2 μ = e^∫P dx" → "3 multiply every term" → "4 integrate: μy = ∫μf dx + C" → "5 divide by μ".

Style: clean flat-vector educational infographic, white background, generous whitespace, muted academic palette (dark slate #263238, teal #00695C, warm grey #78909C, soft fill #EEF3F6), crisp sans-serif labels, no 3D, no photorealism, no gradients, no decorative clutter. Render ALL text exactly as written between quotes — do not invent extra text or change any formula. Aspect ratio 16:9.

Final revision instructions (applied to generated draft):
Edit this educational infographic only to fill the entire 16:9 canvas with opaque solid pure white, including all empty areas between and outside panels, so every title and diagram is clear against white. Preserve all existing correct text, math, labels, layout, diagram connections, sizes and teal/slate colors. Do not use transparency, alpha cutouts, shadows or gradients. Also make two precise mathematical accuracy corrections: replace the left caption "not the derivative of anything — cannot integrate directly" with exactly "not generally a single product derivative — use an integrating factor". On the right, the collapsed result must read "(μy)′ = μf(x)" rather than only "(μy)′. Make clear that the left side μy′ + μP(x)y equals (μy)′. Preserve the correct product-rule check and all five numbered steps.
