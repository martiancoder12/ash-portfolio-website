Create a flat-vector educational diagram titled "Voltage Divider: the Bigger Resistor Takes the Bigger Share". Left side: a flat series circuit with a source "Vs" and two zigzag resistors "R1" and "R2", with "+ V2 −" marked across R2. Right side: one horizontal bar labeled "Vs" split into two segments proportional to R1 and R2; the R2 segment highlighted in teal and labeled "V2 = Vs · R2 / (R1 + R2)". Caption under the bar: "split by resistance ratio — R_target over R_TOTAL, never R_target over the other resistor". Small example chip at the bottom: "24 V across 4 kΩ + 8 kΩ → V2 = 16 V".

Style: clean flat-vector educational infographic, white background, generous whitespace, muted academic palette (dark slate #263238, teal #00695C, warm grey #78909C, soft fill #EEF3F6), crisp sans-serif labels, no 3D, no photorealism, no gradients, no decorative clutter. Render ALL text exactly as written between quotes — do not invent extra text or change any formula. Aspect ratio 16:9.

Final revision instructions (applied to generated draft):
Edit this educational voltage-divider image to correct only the proportional bar: the grey R1 segment must be exactly ONE THIRD of the bar width and the teal R2 segment exactly TWO THIRDS. This matches the existing example 4 kΩ + 8 kΩ, where V2=16 V out of Vs=24 V. Keep the correct formula V2 = Vs · R2 / (R1 + R2), all existing labels, caption, circuit topology, polarity and example unchanged. Fill the entire canvas opaque pure white, including all areas outside panels. 16:9. No transparency, no alpha cutouts, no gradients, no shadows.
