Create a flat-vector educational diagram titled "Every Linear Solution = y_c + y_p". Center top: large equation "y = y_c + y_p". Two branches below. Left branch to a box labeled "y_c = C·e^(−∫P dx)" with subtitle "homogeneous part — solves y' + Py = 0 · carries the constant C · shaped by the initial condition". Right branch to a box labeled "y_p" with subtitle "particular part — any one solution driven by f(x) · the forced response". Bottom: an example bar for "y' − 3y = 6" showing "y = C·e^(3x) + (−2)" with the two pieces color-coded to match their branch boxes.

Style: clean flat-vector educational infographic, white background, generous whitespace, muted academic palette (dark slate #263238, teal #00695C, warm grey #78909C, soft fill #EEF3F6), crisp sans-serif labels, no 3D, no photorealism, no gradients, no decorative clutter. Render ALL text exactly as written between quotes — do not invent extra text or change any formula. Aspect ratio 16:9.

Final revision instructions (applied to generated draft):
Edit this educational image only to restore its missing title at the top, exactly "Every Linear Solution = y_c + y_p", and make the entire background opaque pure white. Preserve all existing correct math, branch labels, colored pieces, text, and the bottom example. Scale content slightly to accommodate the missing title with clear generous whitespace and no overlaps. 16:9 aspect ratio. No transparency, no alpha cutouts, no gradients, no shadows.
